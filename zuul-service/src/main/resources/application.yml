server:
  port: 8001

eureka:
  client:
    serviceUrl:
      defaultZone: http://eurekaservice.phoenix.local:8901/eureka/

spring:
  application:
    name: zuulservice
  boot:
    admin:
      client:
        url: http://localhost:8084
  zipkin:
    base-url: http://localhost:9411

zuul:
  routes:
    api-account:
      path: /api/account/**
      serviceId: accountweb
